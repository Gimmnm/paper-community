# Paper-community: offline pipeline only (embed / graph / sweep / eval without FastAPI).
# Web demo additionally needs: pip install -r requirements-api.txt
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
pyreadr>=0.5
python-igraph>=0.11
leidenalg>=0.10
pillow>=10.0
imageio>=2.31
imageio-ffmpeg>=0.4

# Common optional backends / viz (uncomment or install as needed)
# umap-learn>=0.5
# pacmap>=0.7
# hnswlib>=0.8
# faiss-cpu>=1.8
# transformers>=4.40
# adapters>=0.10
# torch>=2.2
