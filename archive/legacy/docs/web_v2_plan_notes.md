# Archived Web v2 Plan Notes

Moved from:

- `docs/web-version-2-plan.md`

Reason:

- The active baseline is `docs/user_manual_zh.md` + `docs/developer_manual_zh.md` + `docs/offline-outputs-catalog.md`; supplemental specs live alongside in `docs/*.md`.
- The older v2 planning note mixed exploratory hierarchy UX and no longer represents
  the current implementation priorities.
