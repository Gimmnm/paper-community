# Archived Hierarchy Tool Scripts

The following scripts were removed from active `src/` surface to keep the refactor
focused on algorithm-to-retrieval workflow:

- `src/hierarchy_viz.py`
- `src/hierarchy_root_trees.py`

They belong to hierarchy-first exploration and are not required by the current
web retrieval baseline.
